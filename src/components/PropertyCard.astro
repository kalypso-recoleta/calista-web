---
import type { CollectionEntry } from 'astro:content';
import { img } from '../lib/cloudinary';
import {
  formatPrecio,
  formatUbicacion,
  formatSuperficie,
  operacionLabel,
  tipoLabel,
  estadoLabel,
} from '../lib/format';

interface Props {
  bien: CollectionEntry<'biens'>;
}
const { bien } = Astro.props;
const d = bien.data;
const portada = d.imagenes[0];
const superficie = formatSuperficie(d);
const inactivo = d.estado === 'vendido' || d.estado === 'alquilado';
---

<article class:list={['card', { inactivo }]}>
  <a href={`/propiedad/${bien.id}`} class="media" aria-label={d.titulo}>
    <img
      src={img(portada, { w: 640, h: 440 })}
      alt={d.titulo}
      width="640"
      height="440"
      loading="lazy"
    />
    <div class="tags">
      <span class="badge">{operacionLabel(d.operacion)}</span>
      {d.desarrollo && <span class="badge">Pozo</span>}
      {
        d.exclusivo && (
          <span class="badge badge--exclusivo">
            <svg width="11" height="11" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
              <path d="M12 2l2.9 6.3 6.9.7-5.1 4.6 1.4 6.7L12 17.8 5.9 20.3l1.4-6.7L2.2 9l6.9-.7z" />
            </svg>
            Exclusivo
          </span>
        )
      }
    </div>
    {
      inactivo && (
        <div class="overlay">
          <span class={`badge badge--${d.estado}`}>{estadoLabel(d.estado)}</span>
        </div>
      )
    }
  </a>

  <div class="body">
    <p class="loc">{tipoLabel(d.tipo)} · {formatUbicacion(d)}</p>
    <h3 class="titulo">
      <a href={`/propiedad/${bien.id}`}>{d.titulo}</a>
    </h3>

    <ul class="specs">
      {d.dormitorios != null && <li>{d.dormitorios} dorm.</li>}
      {d.banos != null && <li>{d.banos} baños</li>}
      {d.cocheras != null && <li>{d.cocheras} coch.</li>}
      {superficie && <li>{superficie}</li>}
    </ul>

    <div class="foot">
      <span class="precio">{formatPrecio(d)}</span>
      <span class="ver">Ver →</span>
    </div>
  </div>
</article>

<style>
  .card {
    display: flex;
    flex-direction: column;
    background: var(--surface);
    border: 1px solid var(--line);
    border-radius: var(--radius);
    overflow: hidden;
    box-shadow: var(--shadow);
    transition: transform 0.18s ease, box-shadow 0.18s ease;
  }
  .card:hover {
    transform: translateY(-3px);
    box-shadow: var(--shadow-lg);
  }
  .card.inactivo {
    opacity: 0.82;
  }

  .media {
    position: relative;
    display: block;
    aspect-ratio: 16 / 11;
    background: var(--violet-soft);
  }
  .media img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .tags {
    position: absolute;
    top: 0.7rem;
    left: 0.7rem;
    display: flex;
    flex-wrap: wrap;
    gap: 0.35rem;
  }
  .tags .badge {
    background: rgba(255, 255, 255, 0.92);
    backdrop-filter: blur(4px);
    box-shadow: 0 2px 8px -4px rgba(0, 0, 0, 0.3);
  }
  .overlay {
    position: absolute;
    inset: 0;
    display: grid;
    place-items: center;
    background: rgba(31, 27, 58, 0.32);
  }
  .overlay .badge {
    font-size: 0.95rem;
    padding: 0.5rem 1.1rem;
    background: #fff;
  }

  .body {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    padding: 1.1rem 1.2rem 1.2rem;
  }
  .loc {
    margin: 0;
    font-size: 0.82rem;
    color: var(--muted);
    letter-spacing: 0.01em;
  }
  .titulo {
    font-size: 1.2rem;
    margin: 0;
    line-height: 1.2;
  }
  .titulo a {
    color: var(--ink);
  }
  .titulo a:hover {
    color: var(--brand);
    text-decoration: none;
  }
  .specs {
    list-style: none;
    display: flex;
    flex-wrap: wrap;
    gap: 0.35rem 0.9rem;
    margin: 0.1rem 0 0;
    padding: 0;
    font-size: 0.86rem;
    color: var(--muted);
  }
  .specs li {
    position: relative;
  }
  .specs li + li::before {
    content: '·';
    position: absolute;
    left: -0.55rem;
    color: var(--line);
  }
  .foot {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    margin-top: 0.6rem;
    padding-top: 0.8rem;
    border-top: 1px solid var(--line);
  }
  .precio {
    font-family: var(--display);
    font-size: 1.35rem;
    font-weight: 600;
    color: var(--brand);
  }
  .ver {
    font-size: 0.85rem;
    font-weight: 600;
    color: var(--violet);
  }
</style>
